public class TestGetResources {
/*
    @Test
    public void getUser() throws NamingException, SQLException {*/
       /* Properties props = new Properties();
        props.put(Context.INITIAL_CONTEXT_FACTORY,"org.jnp.interfaces.NamingContextFactory");
        props.put(Context.URL_PKG_PREFIXES, "com.sun.enterprise.naming");
        props.put(Context.PROVIDER_URL, "http://localhost:4848/");

        InitialContext initialContext;

            initialContext = new InitialContext(props);

        UserService ds = (UserService) initialContext.lookup("java:app/MyOrderDB");
        Connection conn = ds.getConnection();
        User user =  ds.findByUserName("username");(User) conn.createStatement().executeQuery("select * from USERS;");
        conn.close();
        try
        {
            initialContext.close();
        } catch(Exception e)
        {
            // eat the exception
        }






    }*/
}
